## Generic PHP Template

A PHP template served using Apache.

## Usage

This template is designed for any simple PHP applications. It supports installing dependencies via Composer if a `composer.json` file is present in the root of your project.

## Definition


??? note "Source"
    ```json
    --8<-- "service_templates/php.json"
    ```

??? note "Dockerfile"
    ```dockerfile
    --8<-- "service_templates/php.Dockerfile"
    ```

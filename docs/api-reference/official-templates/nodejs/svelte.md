## Sveltekit Template

A template for deployinng [SvelteKit](https://svelte.dev/) applications.

## Usage

This template uses the [Node adapter](https://svelte.dev/docs/kit/adapter-node) for SvelteKit. Installs packages via npm.

## Definition


??? note "Source"
    ```json
    --8<-- "service_templates/svelte.json"
    ```

??? note "Dockerfile"
    ```dockerfile
    --8<-- "service_templates/svelte.Dockerfile"
    ```

{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to svs-core docs","text":"<p>API Reference</p>"},{"location":"api-reference/","title":"API reference","text":"<p>User</p> <p>SSHKey</p>"},{"location":"api-reference/ssh-key/","title":"SSHKey","text":""},{"location":"api-reference/ssh-key/#svs_core.users.ssh_key.SSHKey","title":"<code>SSHKey(id, name, content, user, *, _orm_check=False)</code>","text":"<p>               Bases: <code>ConstructableFromORM</code></p> <p>Represents an SSH key associated with a user.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>int</code> <p>The unique identifier of the SSH key.</p> <code>name</code> <code>str</code> <p>The name of the SSH key.</p> <code>content</code> <code>str</code> <p>The content of the SSH key.</p> <code>user</code> <code>User</code> <p>The user associated with the SSH key.</p> Source code in <code>svs_core/users/ssh_key.py</code> <pre><code>def __init__(\n    self, id: int, name: str, content: str, user: User, *, _orm_check: bool = False\n):\n    super().__init__(_orm_check=_orm_check)\n\n    self.id = id\n    self.name = name\n    self.content = content\n    self.user = user\n</code></pre>"},{"location":"api-reference/ssh-key/#svs_core.users.ssh_key.SSHKey.is_valid","title":"<code>is_valid(name, content)</code>  <code>staticmethod</code>","text":"<p>Check if the SSH key is valid. The name must be between 1 and 32 characters long. The content must be a valid SSH key format.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the SSH key.</p> required <code>content</code> <code>str</code> <p>The content of the SSH key.</p> required <p>Returns:     bool: True if the SSH key is valid, False otherwise.</p> Source code in <code>svs_core/users/ssh_key.py</code> <pre><code>@staticmethod\ndef is_valid(name: str, content: str) -&gt; bool:\n    \"\"\"Check if the SSH key is valid.\n    The name must be between 1 and 32 characters long.\n    The content must be a valid SSH key format.\n\n    Args:\n        name (str): The name of the SSH key.\n        content (str): The content of the SSH key.\n    Returns:\n        bool: True if the SSH key is valid, False otherwise.\n    \"\"\"\n    if not name or len(name) &gt; 32:\n        return False\n\n    if len(content) &gt; 4096:\n        return False\n\n    ssh_key_pattern = r\"^(ssh-(rsa|dss|ed25519|ecdsa) AAAA[0-9A-Za-z+/]+[=]{0,3} .+)|(ecdsa-sha2-nistp[0-9]+ AAAA[0-9A-Za-z+/]+[=]{0,3} .+)$\"\n    match = re.match(ssh_key_pattern, content)\n\n    return bool(match)\n</code></pre>"},{"location":"api-reference/ssh-key/#svs_core.users.ssh_key.SSHKey.delete","title":"<code>delete()</code>","text":"<p>Delete the SSH key from the database and remove it from the user.</p> Source code in <code>svs_core/users/ssh_key.py</code> <pre><code>def delete(self) -&gt; None:\n    \"\"\"Delete the SSH key from the database and remove it from the user.\"\"\"\n    self.user.ssh_keys.remove(self)\n\n    SideEffectAdapter.dispatch_delete_ssh_key(self.user, self)\n    DBAdapter.delete_ssh_key(self.user, self)\n    self.user.ssh_keys.remove(self)\n</code></pre>"},{"location":"api-reference/user/","title":"User","text":""},{"location":"api-reference/user/#svs_core.users.user.User","title":"<code>User(id, name, ssh_keys=[], *, _orm_check=False)</code>","text":"<p>               Bases: <code>ConstructableFromORM</code></p> <p>Represents a user in the system.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>int</code> <p>The unique identifier of the user.</p> <code>name</code> <code>str</code> <p>The name of the user.</p> <code>ssh_keys</code> <code>list[SSHKey]</code> <p>The list of SSH keys associated with the user.</p> Source code in <code>svs_core/users/user.py</code> <pre><code>def __init__(\n    self,\n    id: int,\n    name: str,\n    ssh_keys: list[\"SSHKey\"] = [],\n    *,\n    _orm_check: bool = False,\n):\n    super().__init__(_orm_check=_orm_check)\n    self.id = id\n    self.name = name\n    self.ssh_keys = ssh_keys\n</code></pre>"},{"location":"api-reference/user/#svs_core.users.user.User.is_username_valid","title":"<code>is_username_valid(username)</code>  <code>staticmethod</code>","text":"<p>Checks if the username is valid for linux systems.\"</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>str</code> <p>The username to check.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>validity of the username.</p> Source code in <code>svs_core/users/user.py</code> <pre><code>@staticmethod\ndef is_username_valid(username: str) -&gt; bool:\n    \"\"\"Checks if the username is valid for linux systems.\"\n\n    Args:\n        username (str): The username to check.\n\n    Returns:\n        bool: validity of the username.\n    \"\"\"\n\n    if not 1 &lt;= len(username) &lt;= 32:\n        return False\n\n    if not re.match(r\"^[a-z_][a-z0-9_-]*[$]?$\", username):\n        return False\n\n    if username.endswith(\"-\"):\n        return False\n\n    return True\n</code></pre>"},{"location":"api-reference/user/#svs_core.users.user.User.create","title":"<code>create(username)</code>  <code>staticmethod</code>","text":"<p>Creates a new user in the system and database. Args:     username (str): The username to create.</p> <p>Returns:</p> Name Type Description <code>User</code> <code>User</code> <p>The created user object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the username is invalid.</p> <code>UserAlreadyExistsException</code> <p>If the user already exists in the system or database.</p> Source code in <code>svs_core/users/user.py</code> <pre><code>@staticmethod\ndef create(username: str) -&gt; \"User\":\n    \"\"\"Creates a new user in the system and database.\n    Args:\n        username (str): The username to create.\n\n    Returns:\n        User: The created user object.\n\n    Raises:\n        ValueError: If the username is invalid.\n        UserAlreadyExistsException: If the user already exists in the system or database.\n    \"\"\"\n\n    if not User.is_username_valid(username):\n        raise ValueError(f\"Invalid username: {username}\")\n\n    if UserManager.name_exists_in_system(username) or UserManager.name_exists_in_db(\n        username\n    ):\n        raise UserAlreadyExistsException(\n            f\"User {username} already exists in system or database.\"\n        )\n\n    SideEffectAdapter.dispatch_create_user(username)\n    return DBAdapter.create_user(username)\n</code></pre>"},{"location":"api-reference/user/#svs_core.users.user.User.delete","title":"<code>delete()</code>","text":"<p>Deletes the user from the system and database.</p> Source code in <code>svs_core/users/user.py</code> <pre><code>def delete(self) -&gt; None:\n    \"\"\"Deletes the user from the system and database.\"\"\"\n\n    SideEffectAdapter.dispatch_delete_user(self)\n    DBAdapter.delete_user(self)\n</code></pre>"},{"location":"api-reference/user/#svs_core.users.user.User.add_ssh_key","title":"<code>add_ssh_key(key_name, key_content)</code>","text":"<p>Adds an SSH key to the user.</p> <p>Parameters:</p> Name Type Description Default <code>key_name</code> <code>str</code> <p>The name of the SSH key.</p> required <code>key_content</code> <code>str</code> <p>The content of the SSH key.</p> required <p>Returns:</p> Name Type Description <code>SSHKey</code> <code>SSHKey</code> <p>The added SSH key object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the SSH key is invalid.</p> Source code in <code>svs_core/users/user.py</code> <pre><code>def add_ssh_key(self, key_name: str, key_content: str) -&gt; \"SSHKey\":\n    \"\"\"Adds an SSH key to the user.\n\n    Args:\n        key_name (str): The name of the SSH key.\n        key_content (str): The content of the SSH key.\n\n    Returns:\n        SSHKey: The added SSH key object.\n\n    Raises:\n        ValueError: If the SSH key is invalid.\n    \"\"\"\n\n    if not SSHKey.is_valid(key_name, key_content):\n        raise ValueError(\"Invalid SSH key data\")\n\n    SideEffectAdapter.dispatch_add_ssh_key(self, key_name, key_content)\n\n    ssh_key = DBAdapter.add_ssh_key(self, key_name, key_content)\n    self.ssh_keys.append(ssh_key)\n\n    return ssh_key\n</code></pre>"}]}